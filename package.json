{
  "name": "perito",
  "version": "1.0.0",
  "description": "This project provides a small Node backend and a static HTML frontend used to practice forensic legal analysis with the OpenAI API.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "frontend": "cross-env VITE_API_BASE_URL=http://localhost:4000 live-server",
    "dev": "concurrently \"npm run start --prefix backend\" \"cross-env VITE_API_BASE_URL=http://127.0.0.1:4000 npx live-server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "live-server": "^1.2.2"
  }
}
